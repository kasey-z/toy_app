<%= form_with(model: miscropost, local: true) do |form| %>
  <% if miscropost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(miscropost.errors.count, "error") %> prohibited this miscropost from being saved:</h2>

      <ul>
      <% miscropost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :context %>
    <%= form.text_area :context, id: :miscropost_context %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :miscropost_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
